@extends('layouts.guest') @section('title',__('ui.login')) @section('content')
<div class="auth-shell"><div class="card auth-card"><div class="auth-brand"><img src="{{ asset('assets/enverif-mark.svg') }}" alt="Enverif"><div><strong>Enverif</strong><div class="small muted">{{ __('ui.tagline') }}</div></div></div><h1>{{ __('ui.login') }}</h1><p>{{ __('ui.no_lock_in') }}</p>
@if(session('status'))<div class="notice good">✓ {{ session('status') }}</div>@endif @if($errors->any())<div class="notice bad">{{ $errors->first() }}</div>@endif
<form method="post" action="{{ route('login.store') }}" class="stack">@csrf<div class="form-group"><label class="form-label">{{ __('ui.email') }}</label><input class="field" type="email" name="email" value="{{ old('email') }}" required autofocus autocomplete="email"></div><div class="form-group"><label class="form-label">{{ __('ui.password') }}</label><input class="field" type="password" name="password" required autocomplete="current-password"></div><div class="between"><label class="inline small muted"><input type="checkbox" name="remember" value="1"> {{ __('ui.remember') }}</label><button class="btn btn-primary" type="submit">{{ __('ui.login') }} →</button></div></form></div></div>
@endsection

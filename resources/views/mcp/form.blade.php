@extends('layouts.app')
@section('title', $server->exists ? __('ui.edit').' '.$server->name : __('ui.add_mcp_server'))
@section('content')
<div class="page-head"><div><h1>{{ $server->exists ? __('ui.edit').' '.$server->name : __('ui.add_mcp_server') }}</h1><p>{{ __('ui.mcp_form_desc') }}</p></div><a class="btn" href="{{ route('mcp.index') }}">{{ __('ui.cancel') }}</a></div>
<form class="card card-pad" method="post" action="{{ $server->exists ? route('mcp.update',$server) : route('mcp.store') }}">@csrf @if($server->exists)@method('PUT')@endif<div class="form-grid"><div class="form-group"><label class="form-label">{{ __('ui.name') }}</label><input class="field" name="name" value="{{ old('name',$server->name) }}" required></div><div class="form-group"><label class="form-label">{{ __('ui.transport') }}</label><select class="select" name="transport"><option value="http">{{ __('ui.streamable_http') }}</option></select></div><div class="form-group full"><label class="form-label">{{ __('ui.endpoint') }}</label><input class="field mono" type="url" name="endpoint" value="{{ old('endpoint',$server->endpoint) }}" placeholder="https://mcp.example.com/mcp" required></div><div class="form-group"><label class="form-label">{{ __('ui.bearer_token') }}</label><input class="field" type="password" name="token" autocomplete="off"><div class="help">{{ $server->exists ? __('ui.credential_kept') : __('ui.optional') }}</div></div><div class="form-group"><label class="form-label">{{ __('ui.protocol') }}</label><select class="select" name="protocol_version"><option value="2025-11-25" @selected(old('protocol_version',data_get($server->configuration,'protocol_version','2025-11-25'))==='2025-11-25')>2025-11-25 · {{ __('ui.stable') }}</option><option value="2026-07-28" @selected(old('protocol_version',data_get($server->configuration,'protocol_version'))==='2026-07-28')>2026-07-28 · {{ __('ui.stateless_preview') }}</option></select></div><div class="form-group full"><div class="switch-row"><div><div class="form-label">{{ __('ui.enabled') }}</div></div><label class="switch"><input type="checkbox" name="enabled" value="1" @checked(old('enabled',$server->exists?$server->enabled:true))><span></span></label></div></div></div><div class="mcp-form-actions form-actions"><button class="btn btn-primary">{{ __('ui.save') }}</button></div></form>
@endsection

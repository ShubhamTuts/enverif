@extends('layouts.app')
@section('title', __('ui.campaigns'))
@section('content')
<div class="page-head"><div><h1>{{ __('ui.campaigns') }}</h1><p>{{ __('ui.campaigns_desc') }}</p></div><a class="btn btn-primary" href="{{ route('campaigns.create') }}">+ {{ __('ui.new_campaign') }}</a></div>
<div class="card"><div class="toolbar"><form method="get"><input class="field" name="q" value="{{ request('q') }}" placeholder="{{ __('ui.search') }}"><select class="select field-sm" name="status" data-auto-submit><option value="">{{ __('ui.status') }}</option>@foreach(['draft','active','paused','completed'] as $s)<option value="{{ $s }}" @selected(request('status')===$s)>{{ ucfirst($s) }}</option>@endforeach</select><button class="btn">{{ __('ui.search') }}</button></form></div>
@if($campaigns->isEmpty())<x-empty :title="__('ui.empty_campaigns')" :href="route('campaigns.create')" :actionLabel="__('ui.new_campaign')" />@else<div class="table-wrap"><table class="table"><thead><tr><th>{{ __('ui.name') }}</th><th>{{ __('ui.status') }}</th><th>{{ __('ui.leads') }}</th><th>{{ __('ui.created') }}</th><th>{{ __('ui.actions') }}</th></tr></thead><tbody>@foreach($campaigns as $c)<tr><td><a class="row-title" href="{{ route('campaigns.show',$c) }}">{{ $c->name }}</a><div class="small muted">{{ Str::limit($c->description,90) }}</div></td><td><x-badge :status="$c->status" /></td><td>{{ $c->leads_count }}</td><td class="small muted">{{ $c->created_at?->format('M j, Y') }}</td><td><a class="btn btn-sm" href="{{ route('campaigns.edit',$c) }}">{{ __('ui.edit') }}</a></td></tr>@endforeach</tbody></table></div><x-pagination :paginator="$campaigns" />@endif</div>
@endsection

@extends('layouts.app')
@section('title', $skill->exists ? __('ui.edit').' '.$skill->name : __('ui.create_skill'))
@section('content')
<div class="page-head"><div><h1>{{ $skill->exists ? __('ui.edit').' '.$skill->name : __('ui.create_skill') }}</h1><p>{{ __('ui.create_skill_desc') }}</p></div><a class="btn" href="{{ route('skills.index') }}">{{ __('ui.cancel') }}</a></div>
<form method="post" action="{{ $skill->exists ? route('skills.update',$skill) : route('skills.store') }}" class="card card-pad">@csrf @if($skill->exists)@method('PUT')@endif<div class="form-grid"><div class="form-group"><label class="form-label">{{ __('ui.name') }}</label><input class="field" name="name" value="{{ old('name',$skill->name) }}" required></div><div class="form-group"><label class="form-label">{{ __('ui.version') }}</label><input class="field mono" name="version" value="{{ old('version',$skill->version ?: '1.0.0') }}" required></div><div class="form-group full"><label class="form-label">{{ __('ui.description') }}</label><input class="field" name="description" value="{{ old('description',$skill->description) }}"></div><div class="form-group full"><label class="form-label">{{ __('ui.capabilities') }}</label><div class="grid grid-3">@foreach(['read','internal_write','external_write','network','secrets','destructive'] as $cap)<label class="inline"><input type="checkbox" name="capabilities[]" value="{{ $cap }}" @checked(in_array($cap,old('capabilities',$skill->capabilities??['read'])))><span>{{ $cap }}</span></label>@endforeach</div></div><div class="form-group full"><label class="form-label">{{ __('ui.body') }}</label><textarea class="textarea mono" style="min-height:380px" name="body" required>{{ old('body',$skill->body) }}</textarea><div class="help">{{ __('ui.skill_body_help') }}</div></div></div><button class="btn btn-primary">{{ __('ui.save') }}</button></form>
@endsection
